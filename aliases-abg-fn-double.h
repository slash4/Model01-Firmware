/* alias to enable double-function layer - must be used with NUMPAD layer >=3
 * NB that this requires each LockLayer layer (e.g. NUMPAD) have at least two
 * further layers above it - otherwise it overflows the layer array on read
 */

#define FN_SHIFT Key_KeymapNext_Momentary
